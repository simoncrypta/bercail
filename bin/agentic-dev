#!/usr/bin/env bash
# Compat shim. The CLI is bercail.
set -euo pipefail
here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [[ -x "$here/bercail" ]]; then
  exec "$here/bercail" "$@"
fi
if [[ -x "${HOME}/.local/bin/bercail" ]]; then
  exec "${HOME}/.local/bin/bercail" "$@"
fi
printf 'error: bercail not found (re-run install.sh)\n' >&2
exit 1
